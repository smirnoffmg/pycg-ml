site_name: pycg-ml
site_description: Add your description here
site_url: https://github.com/owner/pycg-ml

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          paths: [src]
          options:
            show_source: true

nav:
  - Home: index.md
  - API: api.md
